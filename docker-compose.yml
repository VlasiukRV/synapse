#docker-compose.yml
version: '3.3'

services:
  synapse:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synapse_core
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    restart: always